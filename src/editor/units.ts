/** 编辑器统一单位制：Babylon 场景中的 1 个世界单位等于 1 米。 */
export const SCENE_UNIT_IN_METERS = 1;

/** 属性面板中展示长度单位的国际符号。 */
export const LENGTH_UNIT_SYMBOL = "m";

/** 属性面板中展示旋转单位的符号。 */
export const ROTATION_UNIT_SYMBOL = "deg";

/** 属性面板中展示缩放倍率的符号，缩放本身没有长度单位。 */
export const SCALE_UNIT_SYMBOL = "x";

/** 工作网格覆盖范围，单位为米。 */
export const EDITOR_GRID_SIZE_METERS = 80;

/** 工作网格每个小格的边长，单位为米。 */
export const EDITOR_GRID_CELL_SIZE_METERS = 1;

/** 默认盒体边长，单位为米。 */
export const DEFAULT_BOX_SIZE_METERS = 1.5;

/** 默认球体直径，单位为米。 */
export const DEFAULT_SPHERE_DIAMETER_METERS = 1.5;

/** 默认圆柱体高度，单位为米。 */
export const DEFAULT_CYLINDER_HEIGHT_METERS = 1.8;

/** 默认圆柱体直径，单位为米。 */
export const DEFAULT_CYLINDER_DIAMETER_METERS = 1.1;

/** 默认地面边长，单位为米。 */
export const DEFAULT_GROUND_SIZE_METERS = 8;

/** 默认点光源抬高距离，单位为米。 */
export const DEFAULT_LIGHT_HEIGHT_METERS = 3;
